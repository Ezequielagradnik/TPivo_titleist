{
  "name": "titan-landing-page",
  "version": "1.0.0",
  "description": "A landing page project with a home section.",
  "main": "src/index.js",
  "scripts": {
    "start": "npm start",
    "build": "npm run build",
    "test": "npm run dev",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  }
}